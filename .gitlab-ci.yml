stages:
  - build
  - test
  - publish

build:
  stage: build
  script:
    - ./build.sh -target=Build

test:
  stage: test
  script:
    - ./build.sh -target=UnitTests -verbosity=diagnostic

publish:
  stage: publish
  script:
    - ./build.sh -target=Publish
